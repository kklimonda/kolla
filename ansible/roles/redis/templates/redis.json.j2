{
    "command": "/usr/bin/redis-server",
    "config_files": [
        {
            "source": "{{ container_config_directory }}/redis.conf",
            "dest": "/etc/redis/redis.conf",
            "owner": "redis",
            "perm": "0644"
        }
    ]
}
